# HTTP port for the backend server
PORT=3001

# Comma-separated list of origins allowed to reach the API during development
CLIENT_ORIGIN=http://localhost:5173

# Required OpenAI API key used for conversations
OPENAI_API_KEY=sk-your-openai-api-key

# Optional override for the model used to chat with the assistant
OPENAI_MODEL=gpt-4o-mini
